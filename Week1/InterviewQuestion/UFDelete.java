public class UFDelete{
    /**
     * Givene a set of n integers S = {0,1,...,n-1} and a sequence of requests of the following form:
     * * Remove x from S
     * * Find the successor of x: the smallest y in S such that y >= x
     * design a datatype so that all operations(except construction) talke logarithmic time or better
     * in the worst case. 
     */
    public static void main(String[] args) {
        
    }
}