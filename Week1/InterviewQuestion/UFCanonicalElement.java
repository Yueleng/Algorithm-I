public class UFCanonicalElement {
    /**
     * Union-find Canonical Element
     * Add a method to the union-find data type so that find(i) returns the 
     * largest element in the connected componennt containing i. The operations,
     * union(), connected(), and find() should all take logarithmic time or better.
     * 
     * 
     */
    public static void main(String[] args) {
        
    }
}